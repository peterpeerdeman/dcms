'use strict';

/* Directives */

var app = angular.module('dcms.directives', []);
app.directive('documentFields', function() {
        alert('ss');
    return {
        restrict:'E',
        template:'<div>ladida</div>'
    }
  });
